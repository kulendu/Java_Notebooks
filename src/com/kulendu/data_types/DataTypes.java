package com.kulendu.data_types;

public class DataTypes {
    public static void main(String[] args) {
        // we will be working on array here
        String[] arr = new String[5];   // declaring an array

        arr[0] = "Hello";   // putting elements in the index
        arr[1] = "my";   // putting elements in the index
        arr[2] = "name";   // putting elements in the index
        arr[3] = "is";   // putting elements in the index
        arr[4] = "Kulendu";   // putting elements in the index

        String arr_ele = arr[2];    // accessig an element
        System.out.println(arr_ele);
    }
}
